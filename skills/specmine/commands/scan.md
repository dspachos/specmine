---
description: Extract requirements from code into .specs/ (survey → excavate → judge → assemble)
argument-hint: "[module or scope — default: confirm after survey]"
---
Load the `specmine` skill and execute its **scan** workflow.

Scope: ${ARGUMENTS:-entire repo — survey first, then confirm the module list with me before the heavy phase}
