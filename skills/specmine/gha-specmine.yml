# specmine gate: lint the .specs/ knowledge base and map PR changes to
# requirements. Runs the in-repo scripts directly — committed with the repo.
# Verdicts (SATISFIED/VIOLATED) are judgment: that's the review agent's job,
# triggered via the specmine skill — CI here enforces the deterministic floor.
name: specmine

on:
  pull_request:
    paths:
      - ".specs/**"
      - "src/**"
      - "lib/**"
      - "**.php"
      - "**.ts"
      - "**.js"
      - "**.py"

permissions:
  contents: read

jobs:
  specs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate .specs (citations, IDs, links, index sync)
        run: node .specmine/scripts/validate.mjs

      - name: Map changed files to requirements
        run: node .specmine/scripts/check.mjs
