# Functional requirements

One file per domain: `auth.md`, `billing.md`, `search.md`, …

IDs are `FR-<DOMAIN>-NNN`, e.g. `FR-AUTH-003`.
Format rules: [../../CONVENTIONS.md](../../CONVENTIONS.md)

Each domain file ends with an `## Open Questions` section.
