# API surface

<!-- Public contracts: HTTP endpoints, CLI commands, events published, exports of a library.
One row per surface: signature, behavior, side effects, source citation.
Changes here are almost always requirement changes — keep this current.

## HTTP

| Method & path | Behavior | Side effects | Sources |
|---|---|---|---|
| POST /v1/orders | Validates and creates an order | Writes DB row, publishes `order.created` | `src/api/orders.ts:42-77` |
-->

_None extracted yet._
